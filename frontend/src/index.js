import React from 'react';
import ReactDOM from 'react-dom'; //Lib utilizada para aplicar o React ao ambiente do browser, que lida com a DOM

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

